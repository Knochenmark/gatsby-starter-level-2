.postCardLink
  text-decoration none

.postCard
  position relative
  color var(--title-color)
  margin-bottom var(--space)

  &TitleLink
    text-decoration none

    h2
      color var(--link-color)
      margin 1rem 0

.postTags
  margin 1rem 0 0
